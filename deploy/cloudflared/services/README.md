# cloudflared Service Units — Not used for feature 004

**Status**: Historical reference only. Feature 004 uses a foreground Quick Tunnel during development/testing.

The feature-003 named-tunnel service notes remain here only for historical traceability. Do not install or enable them for feature 004.

For the supported test procedure, use [`../quick-tunnel.md`](../quick-tunnel.md). It intentionally has no service unit, named tunnel, credential, DNS, or custom hostname.

Automatic service installation may be designed later only after a separately approved production deployment requirement and an inspected origin operating system.
