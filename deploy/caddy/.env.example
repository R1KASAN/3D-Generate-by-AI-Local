# Local single-node Caddy configuration only. This file contains no secrets,
# public hostname, DNS value, tunnel token, or second-server address.

WEB_UPSTREAM=http://127.0.0.1:3000
API_UPSTREAM=http://127.0.0.1:8000
CADDY_LOG_PATH=./logs/caddy-access.log
CADDY_MAINTENANCE_ROOT=./deploy/caddy/maintenance
