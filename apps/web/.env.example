# Mango74 static export configuration (replace only with approved values).
MANGO74_STATIC_EXPORT=1
NEXT_PUBLIC_BASE_PATH=/mango74
NEXT_PUBLIC_API_BASE_URL=https://mango74-api.mangosgo.com/api/v1

# Optional preview-only label; never use a temporary Tunnel URL here.
NEXT_PUBLIC_DEPLOYMENT_ENV=production
